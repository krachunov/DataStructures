package homeWork4;

import java.util.List;
import java.util.Map.Entry;
import java.util.Scanner;
import java.util.TreeMap;

public class TreeTest {

	@SuppressWarnings({ "unused", "unchecked", "unused", "rawtypes", "unused" })
	public static void main(String[] args) {

		// use this if you want to add manualy tree node

		// Scanner sc = new Scanner(System.in);
		// System.out.println("Enter the num ");
		// String nodeCount = sc.nextLine();
		// int n = Integer.parseInt(nodeCount);
		// for (int i = 0; i <= n; i++) {
		// System.out.println("Enter the lin ");
		// String line = sc.nextLine();
		// String[] edge = line.split(" ");
		// int parentValue = Integer.parseInt(edge[0]);
		// Tree<Integer> parentNode = null;
		// parentNode = parentNode.getTreeNodeByValue(parentValue);
		// if (edge.length > 1) {
		// int childValue = Integer.parseInt(edge[1]);
		// Tree<Integer> childNode = null;
		// childNode = childNode.getTreeNodeByValue(childValue);
		// parentNode.setChildren(childNode);
		// childNode.setParent(parentNode);
		// }
		// }

		// use this if you dont want to add manualy tree node
//		Tree<Integer> myTree = new Tree<Integer>(7, new Tree<Integer>(19,
//				new Tree<Integer>(1), new Tree<Integer>(12), new Tree<Integer>(
//						31)), new Tree<Integer>(21), new Tree<Integer>(14,
//				new Tree<Integer>(23), new Tree<Integer>(6)));
		
//		Tree<Integer> root = (Tree) Tree.findeRootNode();
//		System.out.println("The value of the first node is " + root.getValue());
//
//		List<Tree<Object>> list = Tree.findMiddleNodes();
//		for (Tree<Object> node : list) {
//			System.out.println("Middle node " + node.getValue());
//		}
//		List<Tree<Object>> listleaf = Tree.allLeafNodes();
//		for (Tree<Object> node : listleaf) {
//			System.out.println("leaf node " + node.getValue());
//		}


	}
}
